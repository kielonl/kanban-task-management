export enum HTTP_METHOD {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum ENDPOINT {
  BOARDS = "/board",
  COLUMNS = "/column",
  TASKS = "/task",
  SUBTASKS = "/subtask",
}
