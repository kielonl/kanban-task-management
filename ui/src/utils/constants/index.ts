export enum HTTP_METHOD {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum ENDPOINT {
  BOARDS = "/board",
  BOARDS_NAMES = "/board/names",
  COLUMNS = "/column",
  TASKS = "/task",
  SUBTASKS = "/subtask",
}
